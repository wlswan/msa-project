server:
  port: 8084

spring:
  application:
    name: comment-service
  redis:
    host: localhost
    port: 6379
  rabbitmq: # 🚨 spring 하위로 정렬
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus" # prometheus 엔드포인트 활성화
  metrics: # 🚨 중요: endpoints와 동일한 레벨로 수정
    tags:
      application: ${spring.application.name}

resilience4j:
  circuitbreaker:
    instances:
      postService:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowType: COUNT_BASED

feign:
  client:
    config:
      default:
        connectTimeout: 2000
        readTimeout: 5000
        decode404: true